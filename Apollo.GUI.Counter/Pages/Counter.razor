@page "/counter"

<h1>Counter</h1>

<p role="status">Current count: @currentCount.CurrentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private Apollo.Counter.Domain.Counter currentCount = new();

    private void IncrementCount() => currentCount.IncrementCount();
}
